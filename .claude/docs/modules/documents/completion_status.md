# Documents (Dosya Yönetimi) — Tamamlanma Durumu

> Bu dosya modülün ne ölçüde tamamlandığını ve spec dışına çıkılan
> durumları raporlar. İlgili her geliştirmede ANINDA güncellenir.
> Status snapshot'tır, kural deposu değil (tam kurallar `business-rules.md`).

**İlerleme:** `▓▓▓▓▓▓▓▓▓▓` %100 (Faz 0-5 / 5 faz tamam)   ·   Status: complete (MVP tamam)   ·   Güncel: 2026-07-05

---

## ✅ Tamamlanan Yapılar

- **Faz 0 — Dev Altyapı (commit `889ce67`):** docker-compose'a Garage (S3-uyumlu, tek-node) + ClamAV servisleri, `docker/garage/garage.toml`, idempotent `scripts/init-garage.sh`, `AWSSDK.S3` paketi (yalnız `Oksis.Infrastructure`), `appsettings.Development.json` `Storage:S3` bölümü.
- **Faz 1 — Domain + Persistence:**
  - `StoredFile` entity'si + `FileStatus`/`VirusScanStatus`/`StorageProviderType` enum'ları + 4 domain event + durum makinesi (12 unit test — commit `feba1e4`).
  - `FileAttachment` polimorfik bağ entity'si (4 unit test — commit `2ac4e07`).
  - `FileCategoryPolicy` kayıt defteri, 6 MVP kategorisi (6 unit test — commit `f799f30`; naming düzeltmesi `_mb`/`_policies` — commit `a401265`).
  - EF persistence: `[files].stored_files` + `file_attachments`, spec § 2.5 index'leri (`ix_stored_files_school_status`, `ix_stored_files_school_checksum`, `ux_stored_files_bucket_object_key`, `ix_file_attachments_entity`) + EF otomatik FK index'i, `stored_file_id` FK Restrict, tenant global query filter, migration `20260704_documents_stored_files` (fiziksel prefix UTC `20260703215538_`; commit `e04653a`).
  - Build + tüm test suite yeşil doğrulandı (2204/2205, 1 bilinen pre-existing flake — Documents ile ilgisiz).
  - Final whole-branch review (2026-07-04): **merge onaylı (Yes)**, sıfır Critical/Important; iki dokunuş uygulandı — csproj yorum konumu + `Confirm` negatif boyut guard'ı + regression testi (13. StoredFile testi — commit `bb49ff1`). Devirler `open-questions.md` OQ-documents-008'de.
- **Faz 2 — Depolama Soyutlaması + S3 + Provisioning:** `IStorageService` abstraction (spec § 3.2 + capability modeli, 3 onaylı multipart eki — commit `2cc17c8`); `S3CompatibleStorageService` Garage entegrasyon testleri 9/9 gerçek uçtan uca (UseChunkEncoding=false + presigned scheme fix — T1'in çalışmayan upload'ını yakaladı — commit `f5c4089`); `ProvisionSchoolBucket` + `SchoolCreatedEvent` otomatik provisioning (post-commit Hangfire, idempotent — commit `ef08bbb`); final review YES (reviewer 10/10 entegrasyonu kendisi yeniden koştu), master merge `d6ce6a9`.
- **Faz 3 — CQRS Yüzeyi + Permissions:**
  - **İzin altyapısı:** `files.*` 6 izin + 20 `role_permission` satırı seed'lendi (SuperAdmin 4, SchoolAdmin 5, Teacher 4, Parent 3, Student 4), `FILES_*` hata kodları + `ResultExtensions.MapStatusCode` bloğu (commit `10acbfa`).
  - **Upload orkestrasyonu:** proxy (`UploadFile`) + iki-fazlı/multipart (`InitiateFileUpload`/`ConfirmFileUpload`), `HashingStreamWrapper` (checksum-sırasında-hesaplama, seekable guard — Garage'a karşı ampirik doğrulandı: non-seekable+known-length SigV4 ile çalışmıyor, guard `!CanSeek`'e genişletildi), RFC 5987 `ContentDispositionBuilder`, `IStorageQuotaService`/`StorageQuotaService` (Redis sayaç + DB SUM reconcile) (commits `9b763f4`/`143e7a7`).
  - **Bağ/yaşam döngüsü + sorgular:** `AttachFile`/`DetachFile`/`DeleteFile` + `IFileAccessGuard`/`FileAccessGuard` (kayıt-defteri, `"School"` çözümleyici canlı, kayıtsız tip → 404) + `GetFileDownloadUrl` (audit `files.download.url-issued` + Content-Disposition override) + `ListFilesByEntity`/`GetSchoolStorageUsage` (`[Cacheable]`)/`GetFileCategoryPolicy` (commit `24bb3d7`); review fix — silme audit logu (`files.delete.soft`) + kota `ReleaseAsync` (commit `00e3b8b`).
  - **`FilesController`:** 10 endpoint (`api/v1/files` proxy/initiate/confirm/attach/detach/delete/download-url/by-entity/usage/policies) + Garage'a karşı iki-fazlı uçtan uca entegrasyon testi (commit `2c3b9a1`).
  - Branch `feature/dosya-yonetimi-faz3` (`10acbfa`→`2c3b9a1`) `master`'a merge edildi (`7c610cd`, 2026-07-04).
- **Faz 4 — Hangfire Job'ları + Log Kataloğu (branch `feature/dosya-yonetimi-faz4`, `0e36ea3`→`038ab75`):**
  - **ClamAV tarama altyapısı (`0e36ea3`/`9212f30`):** `nClam` ile `IVirusScanner` + tri-state `VirusScanVerdict {Clean, Infected, Skipped}`. **Fail-closed DI gate:** `ClamAv:Enabled=false` → `NullVirusScanner` (yalnız bilinçli operatör opt-out'unda `Skipped`); `Enabled=true` → HER ZAMAN `ClamAvScanner` kaydedilir — startup ping probu yalnız gözlemlenebilirlik amaçlı, ClamAV daemon çöktüğünde tarama anında `VirusScanException` fırlatır (Hangfire retry, dosya `Quarantined` kalır, ASLA otomatik-temiz olmaz).
  - **`VirusScanJob` (`2f3c9aa`):** `FileUploadConfirmedEvent` tetikli; `ForwardHashingStream` ile indirme+tarama+SHA-256 hesaplama TEK geçişte (RAM'e tam yükleme yok, ClamAV INSTREAM'in forward-only okuma davranışı ampirik doğrulandı). Aynı geçişte spec § 3.4'ün checksum-mutabakat noktası: presigned yüklemede istemci-beyan checksum burada gerçek içerikle karşılaştırılır, uyuşmazlıkta `Quarantined` + Critical log. Sonuç `IFileScanStatusPusher` ile SignalR `school:{schoolId}` grubuna push'lanır (`NotificationHub`'a eklenen yeni okul-geneli broadcast grubu).
  - **Cross-tenant sweep job'ları (`8a744ac`):** `ExpireRoleAssignmentsJob` kalıbı — `OrphanUploadCleanupJob` (saatlik; terk edilmiş `PendingUpload` DB+storage temizliği + kota `ReleaseAsync`), `SoftDeletePurgeJob` (günlük 04:00; 30 gün aşan `SoftDeleted` kayıtları `ExecuteDeleteAsync` ile kalıcı siler + `data.delete.kvkk` logu; **kota `ReleaseAsync` ÇAĞIRMAZ** — ctor'da `IStorageQuotaService` bile yok, çifte-release yapısal olarak imkânsız), `RetentionEnforcementJob` (günlük 04:20; kategori `RetentionPeriod` + sezon bitişini aşan `Active` dosyaları `SoftDeleted`'a çeker + kota release). Bu task sırasında Faz 3 bug'ı bulunup düzeltildi: `StoredFile.MarkSoftDeleted()` artık `DeletedAt` parametresini set ediyor (önceden hiç yazılmıyordu — `SoftDeletePurgeJob`'ın adayı asla bulamamasına yol açan bir engeldi).
  - **`ThumbnailGenerationJob` (`17b073c`/`038ab75`):** `FileUploadConfirmedEvent` (görsel/pdf) tetikli; SkiaSharp (resize) + PDFtoImage (PDF ilk sayfa) ile ~320px önizleme üretir, `StoredFile.ParentFileId` ile ayrı bir `StoredFile` (yeni `Thumbnail` kategorisi, `RequiresVirusScan=false`) olarak kaydeder. Çift katman sonsuz-döngü koruması (enqueuer + job, thumbnail'ın kendisi için ikinci thumbnail üretilmez). 25 MB üst-boyut kapısı (`FileUploadThresholds.ProxyMaxSizeBytes` ile aynı sabit) — kaynak dosya bundan büyükse indirmeden no-op (`files.thumbnail.skipped-oversize` logu), büyük `VirtualBook` PDF'lerinin OOM riskini kapatır.
  - **Log kataloğu (spec § 8.2) tamamlandı:** `files.upload.initiated`/`files.upload.confirmed` (retroaktif, initiate/confirm handler'larına eklendi), `files.scan.clean`/`files.scan.infected`, `files.upload.orphan-cleaned`, `files.purge.physical` (`data.delete.kvkk`), `files.retention.enforced` (spec tablosunda olmayan ek tutarlılık kaydı), `files.thumbnail.skipped-oversize`. Tüm satırlar structured (string-interpolation yok), presigned URL/query-string loglanmaz.
  - Final review'da açılan Important bulgu (thumbnail RAM buffer OOM riski) fix round 1 (`038ab75`) ile kapatıldı.
  - Branch `master`'a merge edildi (`e1c8bc7`, 2026-07-04).
- **Faz 5 — Logo Göçü + Web + Eski Servis Emekliliği (api branch `feature/dosya-yonetimi-faz5`, `76686f2`→`3fe10aa`; web branch `feature/dosya-yonetimi-faz5`, `a78ade0`→`f96abcc`; henüz master'a merge edilmedi):**
  - **Task 1 — `FileAccessGuard` intent-aware (`76686f2`, KTK-2):** Faz 4 final review'un School yazma-kapsamı blocker'ı kapatıldı. `IFileAccessGuard.CanAccessAsync(entityType, entityId, FileAccessIntent Read|Write, ct)`; `SchoolEntityScopeResolver` Read → tenant-wide (`CurrentSchoolId==entityId`), Write → `school-settings.upload-logo` izni (yalnız SchoolAdmin'de; SuperAdmin bypass) — `files.delete`in SchoolAdmin+Teacher+Student'a ortak verilmiş olması nedeniyle ayırt edici sinyal olarak kullanılamadığı için bu izin seçildi. 5 call site güncellendi (Attach/Detach/Delete→Write, GetDownloadUrl/List→Read); yetkisiz Write → 404 (kaynak sızdırmama, spec §4.1.4). Application 1352/1352, Infrastructure entegrasyon 64/64 (Documents alt kümesi).
  - **Task 2 — SchoolLogo göçü + public logo proxy (`a548bda`, KTK-1):** `SchoolSettings.LogoStoredFileId Guid?` + `UpdateLogo(Guid?)` (migration `20260704_school_settings_logo_stored_file_id`, FK yok — cross-schema, `StoredFile.ParentFileId` emsali). `UploadSchoolLogoCommandHandler`/`DeleteSchoolLogoCommandHandler` artık `UploadFileCommand`(SchoolLogo)+`AttachFileCommand`("School")+`DeleteFileCommand` ile yeni Documents altyapısına yazıyor; eski `IFileStorageService` çağrıları bırakıldı. Yeni `PublicSchoolLogoController` → `GET /api/v1/public/schools/{schoolId}/logo` `[AllowAnonymous]` (pre-login logo — login sayfası branding, davet önizleme); handler `IgnoreQueryFilters()` + çift `SchoolId`/`Id` eşitliğiyle (cross-tenant sızıntı imkansız, test kanıtlı) logoyu bulur, yalnız Active+Clean ise stream eder, aksi halde 404 (`no-store`, başarı yolu `max-age=300`). Tek URL yardımcısı `ISchoolLogoUrlBuilder` 3 okuma noktasında (`SchoolSettingsDetailDto`, `SchoolBrandingDto`, `InvitationTokenPreviewDto`) `LogoUrl`'ü `LogoStoredFileId`'den türetir; `Theme.LogoUrl` alanı KALDI ama yönetilen akış artık ona yazmaz (derive-at-read, eski elle-yazılmış logolar için legacy-fallback güvenlik ağıyla). **Kritik veri-kaybı bulgusu + fix (`d5cfef9`, fix round 1):** eski akış önce mevcut logoyu soft-delete edip commit ediyor, SONRA yeni dosyayı yüklüyordu — reddedilen yeniden-yükleme (ör. artık kabul edilmeyen JPEG) `Result.Failure` döndüğünden `TransactionBehavior` rollback yapmıyor, mevcut logo kalıcı olarak yok oluyordu. Sıra değişti: yükle→attach→`UpdateLogo` (yeni logo canlı) →(ancak BURADA) eski logo best-effort soft-delete. Prove-It testiyle (RED→GREEN) doğrulandı.
  - **Task 3 — Eski servis emekliliği / B1 (`3fe10aa`):** `IFileStorageService.cs`/`FileStorageService.cs`/`FileStorageOptions.cs` SİLİNDİ, DI kaydı (`Configure`+`AddSingleton`) ve `appsettings.json` `FileStorage` bölümü kaldırıldı. `grep -rn "IFileStorageService\|FileStorageService\|FileStorageOptions" src tests` → **BOŞ** (controller-doğrulanmış). Build 0 warning/0 error; Application 1353/1353, Infrastructure 419/420 (tek bilinen `PersonDirectoryChildren` flake, ilgisiz).
  - **Task 4 (web, `a78ade0`):** `src/shared/files` katmanı — `FileUpload.tsx` (react-dropzone, `files.upload` izin-yoksa-render-yok, client policy re-validation), `files.api.ts` (9 endpoint), `files.keys.ts`, `useFileUpload`(+attach adımı)/`useEntityFiles`/`useFilePolicy`, `files.types.ts` (Zod), `files.json` (tr+en). Hedef katman 12/12, `src/shared/**` 117/117, `npm run build` yeşil.
  - **Task 5 (web, `f96abcc`):** `GeneralTab` (Genel Bilgiler sekmesi) logo bölümü migrated akışa bağlandı — mevcut `useUploadLogo`/`useDeleteLogo` (aynı `/school-settings/logo` endpoint'i, backend şekli korudu) + `logoVersion` cache-buster (`?v=`) upload/delete `onSuccess`'te bump'lanır (proxy URL stabil olduğundan tarayıcı 5dk cache'i kırmak için), accept PNG/SVG-only (JPEG çıkarıldı, sunucu 422 `FILES_POLICY_VIOLATION` ile ikinci savunma hattı), karantina-penceresi `<img onError>` → placeholder fallback (tarama bitince `logoSrc` değişince otomatik yeniden dener). `shared/components/files/FileUpload.tsx` bilinçli olarak KULLANILMADI (mevcut `.gnl-logo-*` handoff-sadık markup + mutation shape'i korumak için minimal wiring tercih edildi). 4/4 (yeni `GeneralTabLogo.test.tsx`) + mutations 9/9 + build/tsc temiz.
  - **Chrome E2E (controller, gerçek tarayıcı):** SchoolAdmin (`mudur.s1@oksis.local`) login → logo, public proxy URL'den 3 yerde (logo kutusu + header + önizleme kartı) görüntülendi → "Kaldır" → 204 → yenilemede temiz. Backend akışı ayrıca curl ile bit-birebir kanıtlandı (upload→scan→public 200→delete→404). Bucket'lar canlı doğrulandı: `oksis-t{SchoolId}` adlandırması (Garage'da 3 tenant bucket'ı).
  - Branch'ler henüz **master'a merge edilmedi** — final review sonrası kontrolcü merge edecek (önce api, sonra web).

## ⏳ Eksik / Bekleyen Yapılar

MVP kapsamı tamam (Faz 0-5 / 5). Kalan her şey post-MVP borç — bkz. aşağıdaki "Known Gaps" ve `open-questions.md` (§11 Açık İşler: KVKK süre teyidi, `files.*` subdomain+TLS prod kurulumu, plan-bazlı kota, FTP adapter, StorageMigrationJob, mobile shared/files, davranışsal tüketiciler).

---

## Known Gaps / Notlar

- **Seeder okulları auto-provision olmaz:** `INotificationEnqueuer` boşluğuyla tutarlı. Faz 3 mitigasyonu: upload öncesi idempotent `EnsureBucket`.
- **Garage entegrasyon test cleanup'ı sessiz catch{}:** leaked-bucket körlüğü (bir kez 29 bucket elle temizlendi). Faz 3 test işinde tek satır teşhis eklenecek.
- **Plan kusuru kaydı:** `MultipartUploadSession.PartUrls` ölü alan (plan T1 imza çelişkisi). Faz 3'te değerlendirilecek.
- **FAZ 4 GUARDRAIL:** `SoftDeletePurgeJob` `ReleaseAsync`'i TEKRAR çağırmamalı — bayt kota sayacından `DeleteFileCommand` anında (soft-delete'te) düşürülmüştür; purge job yalnız fiziksel S3 nesnesini siler, kota rezervasyonuna dokunmaz (bkz. Faz 3 Task 3 fix-round-1).
- **`GetFileCategoryPolicy` Tenancy-Required:** query şu an `[Tenancy(Required)]` + `files.view` gerektiriyor; anonim/login-öncesi policy okuma ihtiyacı doğarsa revisit edilecek. Faz 5 kapsamında ele alınmadı — post-MVP backlog'da kaldı.
- **✅ ÇÖZÜLDÜ (Faz 5 Task 1, `76686f2`) — School yazma-kapsamı (eski final review Important #2):** `SchoolEntityScopeResolver` artık intent-aware (`FileAccessIntent Read|Write`) — School: Read=tenant-wide, Write=yalnız `school-settings.upload-logo` izni (SchoolAdmin) + SuperAdmin bypass. Student/Teacher/Parent artık School'a yazamaz (spec §4.1.3 ihlali kapandı).
- **FAZ 4 devri — orphan job kota semantiği (final review Minor #8):** başarısız confirm rezervasyonu tutar; `OrphanUploadCleanupJob` ya `ReleaseAsync` çağırır YA DA TTL reconcile'a bırakır — İKİSİ BİRDEN double-subtract eder. Ayrıca `StoredFile` multipart `UploadId` saklamıyor → orphan job terk edilmiş session'ları `ListMultipartUploads` ile abort etmeli (Garage destekliyor). Faz 5'te ele alınmadı, post-MVP backlog'da kaldı.
- **Faz 5 devri — logo "Kaldır" sonrası anlık tarayıcı cache'i (minor UX):** cache-buster (`?v=`) yalnız upload/delete `onSuccess`'inde bump'lanır; proxy URL stabil olduğundan bazı durumlarda tarayıcı "Kaldır" sonrası bir önceki logoyu kısa süre göstermeye devam edebilir, sayfa yenilemede düzelir. Veri doğru (backend'de `LogoStoredFileId=null`), yalnız görsel anlık-yenileme UX'i. Post-MVP backlog.
- **Faz 5 devri — SuperAdmin School-write dalı bugün erişilemez:** `SchoolEntityScopeResolver` Write yolunda `ITenantContext.IsSuperAdmin` bypass'ı var ama mevcut RBAC seed'inde SuperAdmin'in `CurrentSchoolId` bir okula eşitlenerek School'a yazması pratikte gerçekleşmiyor (SuperAdmin cross-tenant upload yapmaz — Faz 5 Task 1 review notu). Dead-ama-zararsız kod yolu; SuperAdmin onboarding/denetim akışı netleşince revisit edilebilir.
- **Faz 5 devri — public logo endpoint'inde rate-limit yok:** `GET /api/v1/public/schools/{schoolId}/logo` `[AllowAnonymous]`, Program.cs'teki global rate-limiter policy'lerine (`[EnableRateLimiting]` opt-in) bağlanmadı (Faz 5 Task 2 review notu). Post-MVP: `anonymous`/`invitation-public` benzeri IP-limit eklenebilir.

- `2026-07-04` — Faz 4 Task 3 (OrphanUploadCleanupJob): plan (faz4-plan Global Constraints) terk edilmiş **multipart session abort** temizliğini (`ListMultipartUploads`+`AbortMultipartAsync`) öngörüyordu; ancak `IStorageService`'te `ListMultipartUploads` YOK ve `StoredFile` `UploadId` saklamıyor (`InitiateFileUpload` yalnız istemciye döndürüyor). Bu deliverable UYGULANMADI (best-effort tek-nesne silme yapıldı). Etki SINIRLI: yalnız presigned (VirtualBook) init-edilip-confirm-edilmeyen nadir durumda terk parçalar object store'da birikir — veri/tenant riski yok; S3/Garage provider lifecycle kuralı da temizleyebilir. **Backlog: `StoredFile.UploadId` kolonu + `IStorageService.ListMultipartUploads` → orphan job abort.** Onay: gece otonomi (sınırlı etki, kod fix ertelendi); sabah kullanıcıya sunulacak.
- **FAZ 4 devri — thumbnail yaşam döngüsü parent'a bağlı değil:** `ThumbnailGenerationJob`'ın ürettiği `StoredFile` (`ParentFileId`) `RetentionPeriod=null` ile kaydedilir; parent `SoftDeletePurgeJob` ile fiziksel silindiğinde thumbnail satırı hem DB'de hem storage'da **yetim kalır** (purge job thumbnail'ı bilmiyor). Backlog — parent silindiğinde ilişkili thumbnail'ların da temizlenmesi ileride eklenecek.
- **FAZ 4 devri — `RetentionEnforcementJob` AcademicSession join'i DB FK'sız:** sezon-bitişi çözümü `StoredFile.AcademicYearId == AcademicSession.Id` join'ine dayanır ama bu alan gerçek bir FK constraint'i değildir (mevcut kod deseni — `ObjectKeyGenerator`/`UploadFileCommandHandler` de aynı örtük ilişkiyi kullanır). `AcademicYearId` yetim kalırsa (referans verilen sezon silinirse) join hiç eşleşmez ve o dosya retention taramasından **sessizce atlanır** (fail-open, hata fırlatmaz).

## ⚠️ Spec Dışına Çıkılanlar

- `2026-07-04` — spec § 3.4 `ProvisionSchoolBucket` "SuperAdmin" izni: repo'da SuperAdmin komut emsali/`TenancyMode.SuperAdminOnly` kullanımı YOK; izin anahtarı uydurulmadı, `[Tenancy(Required)]` + handler tenant-guard uygulandı, HTTP endpoint'i yok. Gelecekteki SuperAdmin onboarding endpoint'inde nitelik kararlaştırılacak. Onay: gece otonomi mandatı + reviewer.
- `2026-07-04` — Faz 3 Task 1 (`files.*` izin altyapısı): permissions.md/faz3-plan matrisindeki "Secretary→view,upload,download" hedef ataması **seed'lenmedi** — Secretary rolü MVP seed'inde yok (Issue #1, 5-rol seti: SuperAdmin/SchoolAdmin/Teacher/Parent/Student). Aynı DUTIES modülü emsali izlendi ("Secretary→duties.view eşlemesi ertelendi"). Diğer 5 rol matrisle birebir seed'lendi (SuperAdmin 4, SchoolAdmin 5, Teacher 4, Parent 3, Student 4). Secretary rolü seed'lendiğinde `RolePermissionSeedData.cs`'e eklenecek. Onay: plan kendisi (dosya-yonetimi-faz3-plan.md Task 1) + kod emsali.
- `2026-07-04` — spec § 3.6.1 **kategori başına kota** uygulanmadı (final review Important #3): spec "okul başına toplam **ve kategori başına** kota" der; plan yalnız okul-toplam kotayı (`FILES_QUOTA_TOTAL_BYTES`) tanımladı, `StorageQuotaService` buna sadık kaldı (plan kusuru → uygulama doğru izledi). Kategori-başına kota gerektiğinde `IStorageQuotaService`'e kategori boyutu eklenir. Onay: plan + reviewer; kod değişikliği ertelendi (tüketici yok).
- `2026-07-04` — spec § 3.4 Confirm **checksum doğrulaması** (final review Important #4): spec "StatAsync ile ... checksum doğrulama" der ama S3 Stat yalnız ETag (MD5/multipart) verir, SHA-256 vermez → presigned upload'da `Sha256Checksum` istemci beyanıdır (shape-only doğrulandı). Ucuz çözüm Faz 4'e verildi: **VirusScanJob zaten nesneyi tam okuyor — aynı geçişte SHA-256 yeniden hesaplayıp saklanan değerle karşılaştırsın, uyuşmazlıkta quarantine/flag.** Boyut yalanı StatAsync'le engellendi; confirm-without-initiate imkânsız (PendingUpload satırı gerekir); cross-tenant confirm global filter'la bloklu. Onay: spec-ifade kusuru + reviewer.
- `2026-07-04` — Faz 4 Task 4: yeni `Thumbnail` kategorisi (`RequiresVirusScan=false`) yalnız **sunucu tarafından üretilir** (`ThumbnailGenerationJob`), istemci tarafından yüklenebilir bir kategori DEĞİLDİR. Registry'de var olması `UploadFileCommand`/`InitiateFileUploadCommand`/`GetFileCategoryPolicyQuery`'nin genel olarak aynı registry'yi kullanmasından dolayı bir istemcinin `category="Thumbnail"` göndererek virüs taramasını atlamasına izin verebilirdi (kendi tasarımının açtığı açık, task içinde bulunup kapatıldı) — 3 handler'a da `CategoryUnknown` guard'ı eklendi (var olan sızdırma-önleme kalıbıyla aynı). Onay: kod tasarımı + reviewer; kalıcı çözüm (ayrı "sistem kategorisi" bayrağı) backlog'da.
- `2026-07-04` — Faz 4 Task 4: `files.upload.confirmed` logunda `ChecksumMatch` alanı **her zaman `true`** yazılır — `ConfirmFileUploadCommandHandler` yalnız `StatAsync` ile boyutu doğrular, byte içeriğini görmez. Gerçek içerik-checksum mutabakatı (spec § 3.4) asenkron olarak `VirusScanJob`'da yapılır; uyuşmazlık orada ayrı `files.scan.infected` (Signature=`CHECKSUM_MISMATCH`) logu ile raporlanır. Bir spec sapması değildir — spec'in "confirm anında" zımni varsayımının, S3 Stat'ın checksum vermemesi nedeniyle Faz 3'te zaten belgelenen kısıtın (bkz. yukarıdaki "Confirm checksum doğrulaması" satırı) log-alanına yansımasıdır. Onay: Faz 3 kararının doğal uzantısı + reviewer.
- `2026-07-04` — **KTK-1 (Faz 5 Task 2):** `SchoolTheme.LogoUrl`'ün anlamı genişletildi — artık stabil **public proxy endpoint**'i (`GET /api/v1/public/schools/{schoolId}/logo`, `[AllowAnonymous]`) işaret ediyor, ham storage URL'i değil. Spec §9 logo göçünü söyler ama public erişim mekanizmasını boş bırakır (login-öncesi branding/davet önizleme presigned+kısa-TTL+izinli akışla çalışmaz); bu bir spec sapması değil, boşluk doldurma genişlemesidir. Onay: dosya-yonetimi-faz5-plan.md KTK-1 + reviewer.
- `2026-07-04` — **KTK-2 (Faz 5 Task 1):** `IFileAccessGuard` intent-aware yapıldı (School Write=SchoolAdmin-only, Read=tenant-wide) — spec §4.1.3'ün "Student yalnız kendi entity'lerine erişir" ilkesini School'a da genişletti; Faz 4 final review'da açılan SERT ÖN KOŞUL/blocker'ı kapattı (bkz. Known Gaps "ÇÖZÜLDÜ" satırı). Onay: dosya-yonetimi-faz5-plan.md KTK-2 + reviewer.
- **Spec §9 (Eski Altyapının Emekliliği) — Uygulandı (Faz 5 Task 3, `3fe10aa`, 2026-07-04):** `IFileStorageService`/`FileStorageService`/`FileStorageOptions` + appsettings `FileStorage` bölümü SİLİNDİ; SchoolLogo `SchoolLogo` kategorisiyle yeni altyapıya göçtü (Task 2); grep ile kod-tarafı sıfır referans doğrulandı. Spec §9 maddeleri 1-3 tam karşılandı.

---

## Branch Notu

Faz 0-1 işi `feature/dosya-yonetimi-faz0-1` branch'inde geliştirildi ve kullanıcı kararıyla doğrudan **master'a merge edildi** (`d752f98`, 2026-07-04, no-ff); branch silindi. Faz 2 de master'a merge oldu (`d6ce6a9`, 2026-07-04). Faz 3, `feature/dosya-yonetimi-faz3` branch'inde (commit'ler `10acbfa`→`2c3b9a1`) tamamlandı ve **master'a merge edildi** (`7c610cd`, 2026-07-04). Faz 4, `feature/dosya-yonetimi-faz4` branch'inde (commit'ler `0e36ea3`→`038ab75`) tamamlandı ve **master'a merge edildi** (`e1c8bc7`, 2026-07-04). **Faz 5**, api `feature/dosya-yonetimi-faz5` branch'inde (base `e1c8bc7`, commit'ler `76686f2`→`3fe10aa`) + web `feature/dosya-yonetimi-faz5` branch'inde (commit'ler `a78ade0`→`f96abcc`) tamamlandı; **her iki repo'da da henüz master'a merge edilmedi** — final review sonrası kontrolcü merge edecek (sıra: api → web, controller-verified Chrome E2E kanıtıyla).
