# Kimlik / Identity — Tamamlanma Durumu

> Bu dosya modülün ne ölçüde tamamlandığını ve spec dışına çıkılan
> durumları raporlar. İlgili her geliştirmede ANINDA güncellenir.
> Status snapshot'tır, kural deposu değil (tam kurallar `business-rules.md`).

**İlerleme:** `▓▓▓▓▓▓▓▓▓▓` %100   ·   Status: complete (API + web + mobile)   ·   Güncel: 2026-06-06 (rol-izin matrisi seviye görünürlük filtresi + /admin/roles ekranı)

> Temel: Mevcut `User` tabanlı auth (login/refresh/invite/password-reset, `Oksis.Application/Modules/Identity` ≈77 cs) + master seed (roller/izinler) çalışır.
> 2026-05-30: Teknik analiz (*Login & Profile Switch · Sürüm 1.0*) docs'a işlendi — domain (Account), api-contracts (switch/me/context), database-schema (identity.accounts/refresh_tokens), permissions (RBAC+ABAC, permission cache), business-rules (TR-auth-001…018), notifications (audit/SignalR), ui-flows, open-questions (TQ-auth-001…007) güncellendi. Hedef model dokümante edildi.
> 2026-05-31: **ISSUE-01 tamamlandı** — `Account` aggregate + `RefreshToken` child entity + `AccountId`/`Identifier`/`NationalIdHash`/`PasswordHash` value objects + 3 yeni enum (`IdentifierType`/`LoginFailureReason`/`LogoutReason`) + 13 domain event + 69 unit test eklendi. OQ-identity-001 kararı: **Option A — Account aggregate kuruldu**, mevcut `User` ile yan yana yaşıyor (Person/Account ayrımı users modülünde tamamlandıkça `User` auth'tan emekli edilecek).
> 2026-05-31: **ISSUE-02 tamamlandı** — `identity.accounts` + `identity.refresh_tokens` tabloları (EF Core Fluent API, ux_accounts_person unique, ix_rt_account_active filtered, ux_rt_token_hash unique, cascade delete) + `Argon2IdPasswordHasher` (TQ-auth-003) + `Sha256IdentityHasher` (refresh/reset token) + `IIdentityHasher` port. Migration `20260531_add_identity_accounts` (yalnız identity şemasını etkiler). 8 persistence integration test + 15 hasher unit test. Legacy User BCrypt hash'leri Argon2 hasher'ın verify fallback'i tarafından kabul edilir (rehash sonra).
> 2026-05-31: **ISSUE-03 tamamlandı** — `IIdentifierResolver` + `IdentifierResolver` (email/phone normalize, login'de TCKN reddi TR-auth-002, recovery'de tenant-tuzlu TCKN hash) + `IPersonDirectory` read-port + `PersonContextView` minimal projeksiyon + `PersonDirectory` adapter (Infrastructure, `INationalIdProtector` aracılığıyla Persons.NationalId.Hash ile aynı HMAC chain). 11 unit test. `LinkedAccountId == null` uniform NotFound.
> 2026-05-31: **ISSUE-06 tamamlandı** — `ILoginGuard` + `LoginGuard` (Redis varsa dağıtık sliding-window, yoksa in-memory; kademeli lockout 5/10/20→5dk/30dk/2sa; IP rate-limit hard 50/5dk) + `AdminUnlockAccountCommand` (RequirePermission("accounts.unlock")) + `AccountErrors`. Account.Unlock yeni `AccountUnlockedEvent` emit eder; audit handler bunu da işler. 10 unit test.
> 2026-05-31: **ISSUE-11 tamamlandı** — `IIdentityPiiMasker` + `IdentityPiiMasker` (email "ah***@d.com", TCKN "•••••••••XX", IPv4 son iki octet, identifier auto-route) + `IdentityAuditHandler` 13+1 Identity domain event'ini structured log'a yazar (Login/Logout/Lock/Unlock/Password/Switch/Reuse/Permission/Suspension); plain PII log'a sızmaz. 21 unit test.
> 2026-05-31: **ISSUE-05 tamamlandı** — `AccountRefreshTokenCommand` (hash → RefreshTokens row → `Account.RotateRefreshToken` → reuse detection zincirin tamamını revoke eder + `SuspiciousTokenReuseEvent` yayar) + `AccountLogoutCommand` (refresh revoke + jti blacklist TTL ≤ kalan ömür) + `AccountLogoutAllSessionsCommand` (Account.RevokeAllRefreshTokens + AllSessionsLoggedOutEvent + jti blacklist). `IAccessTokenBlacklist` (Redis `blacklist:jti:{jti}` + in-memory fallback) + `IAccessTokenContext` (JWT jti/exp/sub/person_id okuyucusu). `JwtBearerEvents.OnTokenValidated` her doğrulanmış isteğe blacklist kontrolü ekledi. Paralel endpoint'ler `/auth/account/{login,refresh,logout,logout-all}` (mevcut User-tabanlı auth korunur). 18 yeni unit test.
> 2026-05-31: **ISSUE-14 tamamlandı** — Account flow için güvenlik regresyon test paketi: `AccountSecurityRegressionTests` (timing-safe Verify çağrı sayısı eşitliği, uniform `InvalidCredentials` kodu NotFound/LinkedAccountId null/BadPassword için, `RedisPermissionCacheBuilder.Key` cross-tenant unique) + `ActiveSeasonWriteHandlerTests` (GET allow / POST current allow / POST past deny / no-claim allow) + `SessionHubGroupNameTests` (aynı accountId farklı okullarda farklı grup → cross-tenant SignalR izolasyonu). Mevcut yerel testlerle (LoginGuard kademeli lockout, AccountRefresh reuse detection, AccountSwitchProfile cache invalidation, AccountSwitchChild ABAC deny, IdentityPiiMasker, ForceLogoutEventConsumer) birlikte tam kapsam. Toplam 702 application + 248 domain + 75 API testi green.
> 2026-05-31: **ISSUE-12 tamamlandı** — `SessionHub` (Microsoft.AspNetCore.SignalR) tenant-scoped `{schoolId}:{accountId}` grubuna otomatik join + `/hubs/session` map. `IForceLogoutNotifier` port + `SignalRForceLogoutNotifier` impl (IHubContext broadcast). `ForceLogoutEventConsumer`: `AllSessionsLoggedOut` + `AccountPasswordChanged` (revoke=true) + `SuspiciousTokenReuse` + `LoginBlockedDueToSuspension` event'lerini tüketir; notifier transient hataları audit'i bloklamaz. Hangfire job iskeletleri (Api'de RecurringJob register'ı): `ExpiredRefreshTokenCleanupJob` (30+ gün), `OtpChallengeCleanupJob` (7+ gün), `AuditRetentionEnforcerJob` (1 yıl KVKK saklama; ELK anonymization Sprint 6 hardening). Redis backplane production paketi sonra eklenecek. 5 unit test.
> 2026-05-31: **ISSUE-13 tamamlandı** — `OtpChallenge` domain entity (`CodeHash`, `OtpPurpose` Login/Reset, `Attempts`/`MaxAttempts`, `ExpiresAt`/`ConsumedAt`; `IsValid` + `RegisterAttempt` + `Consume`) + `identity.otp_challenges` tablosu + `20260531_add_identity_otp_challenges` migration. `ISmsSender` port (concrete impl yok). `RequestLoginOtpCommand` + `VerifyLoginOtpCommand` iskeleti — `OtpFeatureOptions.LoginEnabled` default off → uniform Failure; Sprint 5'te akış bağlanır. `TwoFactorEnabled` Account aggregate'inde hazır (ISSUE-01). 8 yeni test (6 domain + 2 application).
> 2026-05-31: **ISSUE-09 tamamlandı** — `AccountSwitchChildCommand` (yalnız Parent profili; FindActiveChildrenAsync + CanViewInfo ABAC kontrolü; deny → `PermissionDeniedEvent` + `UnlockUnauthorized`; başarılı switch JWT'yi değiştirmez, `IChildSessionStore` Redis `session:{jti}:childId` TTL kalan access ömrü) + `AccountSwitchSeasonCommand` (geçmiş sezona `seasons.view-archived` permission gerektir; başarılı → cache invalidate + `Account.RecordActiveSeason` + context re-resolution + cache rebuild + yeni JWT). `ChildScopeRequirement` + handler iskeleti (ABAC > RBAC; policy `child-scope`) + `ActiveSeasonWriteRequirement` + handler iskeleti (policy `active-season-write`; GET/HEAD/OPTIONS pas, write'larda `active_season_id != current` red). `IChildSessionStore` port + Redis + InMemory fallback. `ContextResolver` sezon semantiği: `LastActiveSeasonId` set ise onu kullan (switch-season tutarlılığı). Endpoint'ler `/auth/account/{switch-child,switch-season}`. 7 unit test.
> 2026-05-31: **ISSUE-08 tamamlandı** — `AccountSwitchProfileCommand` + handler + validator: hedef profil JWT'deki `available`'larda değilse 400 → eski cache key invalidate → `Account.RecordActiveProfile` → context re-resolution → `BumpPermissionsVersion` → yeni cache build → yeni JWT + refresh. `IAccountPermissionResolver` (Persons.RoleAssignment + identity.role_permissions join'i, aktif profile → SystemRole.PortalType filtresi). `RedisPermissionCacheBuilder` (key `permissions:{accountId}:{profileType}:{seasonId}`, TTL 30dk; ISSUE-04 NoopPermissionCacheBuilder yerine Redis varken kayıt olur). `/auth/account/switch-profile` endpoint'i. 4 unit test.
> 2026-05-31: **ISSUE-10 tamamlandı** — Account-tabanlı parola kurtarma akışları: `AccountForgotPasswordCommand` (TR-auth-008 uniform 202, `IIdentifierResolver.FindForRecoveryAsync`, 32-byte token + SHA-256 hash, 30dk TTL, Email kanalı Hangfire enqueue) + `AccountResetPasswordCommand` (token consume + PasswordPolicy + `Account.ChangePassword(revokeRefreshTokens=true)`) + `AccountChangePasswordCommand` (mevcut parola verify + tüm refresh revoke). `AccountPasswordResetToken` domain entity + `PasswordResetChannel` enum + `identity.account_password_reset_tokens` tablo (migration 20260531064146; `ux_aprt_token_hash` unique, `ix_aprt_account_active` filtered). `PersonContextView.PrimaryEmail` opsiyonel alanı eklendi. Endpoint'ler `/auth/account/{forgot-password,reset-password,change-password}` (mevcut User akışı korunur). 12 unit test.
> 2026-05-31: **ISSUE-07 tamamlandı** — `ContextResolver` tam impl (`DefaultContextResolver` stub'ı yerine): 1 profil auto / çoklu + geçerli `LastActiveProfileType` auto / aksi halde `NeedsProfileSelection`; Parent ise 1 çocuk auto, `LastActiveChildId` hint → o, çoklu+hint yok → null. Sezon: `ICurrentSessionProvider` → `School.CurrentSeason` fallback `Account.LastActiveSeasonId`. `ConsentGate` tam impl: yürürlükteki `ConsentBundle` vs son `DataProcessing` `ConsentRecord` (no-consent/revoked/bundle-mismatch deny → 403). `IPersonDirectory.FindActiveChildrenAsync` read-port (ParentStudentRelationship). `GET /auth/me/context` + `GET /auth/me/available-contexts` endpoint'leri + `GetCurrentContextQuery`/`GetAvailableContextsQuery` handler'ları. 19 unit test.
> 2026-05-31: **ISSUE-04 tamamlandı** — `AccountLoginCommand` + handler + validator + `AccountAuthResult` (Section 18.1 akışı: classify → resolve → guard → DB account → password verify → lifecycle gate → consent gate → context resolve → permission cache → token issue → `RegisterSuccessfulLogin`). TR-auth-002 TCKN reddi, TR-auth-004 uniform 401 + 403 `ACCOUNT_SUSPENDED` istisna, 409 `NEEDS_PROFILE_SELECTION` (çoklu profil + auto-select edilemedi). Sabit dummy Argon2id verify (timing-attack korumasi). Yeni portlar: `IContextResolver`/`IConsentGate`/`IPermissionCacheBuilder`/`IAccountTokenIssuer`; stub'lar (ISSUE-07/08 gerçek impl): `DefaultContextResolver` (1 profil → auto; LastActiveProfile fallback), `NoopConsentGate`, `NoopPermissionCacheBuilder`. `AccountTokenIssuer` JWT claim seti (sub=accountId, person_id, school_id, perms_ver, active_profile_type, available_profiles, active_child_id?, active_season_id?, jti). `PerformanceBehavior` >500ms warn pipeline'a eklendi. `/api/v1/auth/account/login` paralel endpoint (mevcut `/auth/login` User-tabanlı akışı bozulmadı). 21 unit test.
> 2026-05-31: **ISSUE-15 tamamlandı (web)** — Account-tabanlı auth veri katmanı: `account.api.ts` (login/refresh/logout/switch-*/me), `account.types.ts`, `account.schema.ts` (TR-auth-002 TCKN reddi), `applyAccountAuthResult` JWT claim çözümü + profile→role + portal home mapper, `useAccountLogin` + 401/403/409/423/429 sınıflandırma. `authStore`'a `AccountContext` (jti, expiresAt, profile, child, season, availableProfiles, requirePasswordChange) eklendi. `httpClient` 401'de Account refresh önceliyor + başarısız refresh'te logout; `refreshTokenManager` single-flight (concurrent 401 → tek refresh). `LoginPage` identifier (email/telefon) tek alan; 409 → `ProfileSelectPage`, 403 → `AccountSuspendedPage` ayrı route. i18n key'leri (TR) tam, hardcoded TR string yok. 21 unit test (account.schema, useAccountLogin error map, single-flight refresh MSW, applyAccountAuthResult mapping). Commit: `2875dda` (oksis-web).
> 2026-05-31: **ISSUE-16 tamamlandı (web)** — Context switcher'lar: `ProfileSwitcher` (availableProfiles > 1 dropdown → switch-profile → cache invalidate), `ChildSwitcher` (Parent only; "Tümü" unified dashboard akışı; switch-child JWT'siz Redis session), `SeasonSwitcher` (switch-season → yeni JWT; geçmiş sezon `isPast` flag), `PastSeasonBanner` + `useReadOnlyForPastSeason` hook (yazma butonları disable). `useSwitchProfile/Child/Season/AvailableContexts` hook'ları. `activeChildStore`/`activeSeasonStore` küçük Zustand store'ları (server state React Query'de kalır). 3 unit test. Commit: `a8d1e11` (oksis-web).
> 2026-05-31: **ISSUE-17 tamamlandı (web)** — Account parola kurtarma akışları: `useAccountForgotPassword` (TR-auth-008 uniform 202; başarı/başarısız ayrımı yapılmaz, kanal sızdırılmaz), `useAccountResetPassword` (tek kullanımlık token + yeni parola → backend tüm refresh revoke), `useAccountChangePassword` (self-servis + force flow ortak hook). `ForgotPasswordPage` (identifier tek alan), `ResetPasswordPage` (URL token), `ForceChangePasswordPage` (RequirePasswordChange zorunlu akış), `ChangePasswordPage` (self-servis, /change-password). RHF/Zod policy mirrored client-side (server otorite). 5 unit test. Commit: `c7a0754` (oksis-web).
> 2026-06-06: **roles-permissions ISSUE-05 tamamlandı (oksis-api)** — *Canlı oturum permVersion senkronu* (eski sprint'in "ISSUE-05"inden ayrı; `.claude/generated-issues/roles-permissions/ISSUE-05`). Mevcut altyapının (perms_ver claim, `Account.BumpPermissionsVersion`, `IAccessTokenBlacklist`, `/auth/me/context` permVersion+perms) üstüne **enforcement döngüsü** eklendi: (1) `IPermissionVersionStore` Redis aynası `permver:{school}:{account}` (+InMemory fallback), token üretiminde (`AccountTokenIssuer`) ve her bump'ta sıcak tutulur; (2) `IPermissionVersionBumper` — kişinin/kişilerin tüm account'larının `PermissionsVersion`'ını artırır, aynayı günceller, acil iptalde refresh token'ları revoke eder (tek sorgu/tek SaveChanges — N+1 yok); (3) `CreateRoleAssignment` bump (oturum kesmez) / `RevokeRoleAssignment` bump + **acil iptal** (refresh revoke); (4) `IPermissionVersionGate` + `JwtBearerEvents.OnTokenValidated` — stale `perms_ver` → **401** (zorunlu refresh), ayna miss'te DB fallback + repopulate; (5) `RolePermissionsChangedEvent` + `INotificationHandler` toplu invalidation (cache + permVersion). 18 yeni unit test (store/bumper/gate/event handler + handler wiring). Acceptance kriterleri AC1-7 karşılandı; sapmalar aşağıda.
> 2026-06-06: **roles-permissions ISSUE-06 tamamlandı (oksis-api)** — *Süresi dolan rol atamalarını temizleyen günlük Hangfire sweep'i* (`.claude/generated-issues/roles-permissions/ISSUE-06`). Issue'nun "sync drift" notu doğrulandı: `RoleAssignment` zaman sınırını desteklemiyordu — önkoşul domain genişletmesi bu issue'ya dahil edildi. (1) **Domain (Users modülü):** `RoleAssignment.ValidUntil` (nullable, UTC), `RoleAssignmentStatus.Expired` (=3), `RoleAssignment.Expire(now)` (yalnız Active→Expired, `RoleAssignmentExpiredEvent` yayar), `Create(...)` opsiyonel `validUntil` parametresi. (2) **Persistence:** `valid_until` kolonu + status check constraint `('Active','Inactive','Expired')`; migration `20260606_add_role_assignment_valid_until`. (3) **Job:** `ExpireRoleAssignmentsJob` (`Infrastructure/BackgroundJobs/Jobs`) — `ExpireStaleInvitationsJob` pattern'i: tek bilinçli global-filter bypass ile aday okulları bulur, her okul için tenant context'i sabitler (cross-tenant sızıntı yok), `ValidUntil < now` aktif atamaları `Expire()` ile çeker, etkilenen her kişide `IPermissionCacheBuilder.InvalidateAllForPersonAsync` + okul bazında tek `IPermissionVersionBumper.BumpForPersonsAsync(revokeSessions: true)` (ISSUE-05 entegrasyonu; N+1 yok), Expired idempotent. Structured logging (sayılar; PII yok). DI'da scoped kayıt; recurring tetikleme (cron config'ten) Api host kurulumunda bağlanacak. 9 yeni test (6 domain + RoleAssignment davranışı, 2 integration: geçmiş→Expired/null+gelecek→değişmez & idempotency, +1 ValidUntil persistence). AC tamamı karşılandı; sapma aşağıda.

> 2026-06-06: **roles-permissions ISSUE-07 tamamlandı (oksis-api)** — *Yetki motoru kabul/QA kapısı* (`.claude/generated-issues/roles-permissions/ISSUE-07`). (1) **Sorgular:** `ListAssignableRolesQuery` (`roles.assign`) — seviye (aktörden kesin düşük) + alt küme (rol izinleri ⊆ aktör izinleri) filtresi; `CreateRoleAssignment` guard'ıyla **birebir (read == write)**, sunucu otoritedir. `ListRolePermissionsQuery` (`roles.manage`) — salt-okunur master rol-izin matrisi. (2) `RolesController`: `GET /api/v1/roles/{assignable,permissions}`. (3) **Yeni izin:** `roles.manage` master kataloğa eklendi (AllPermissionIds → SuperAdmin+SchoolAdmin) + migration `20260606_add_roles_manage_permission`. (4) **Motor düzeltmesi:** `RevokedAt == null` → `Status == Active` (AccountPermissionResolver, CreateRoleAssignment actorLevels, RolePermissionsChangedEventHandler fan-out) — ISSUE-06 ile eklenen `Expired` atamalar `RevokedAt==null` olduğundan **yetki/seviye vermeye devam ediyordu**; bu gerçek bir privilege-leak'ti, kapatıldı + regression testi. 12 yeni test (3 app-unit 403 kapısı + 9 integration: §7.1/7.2/7.3, default-deny, expired-no-grant, assignable filtre, matris). **Sapma/derinlik notu:** §7.1 "ABAC" MVP'de **scope-persistence** seviyesindedir (genel `IScopeEvaluator` yok); §7.2 "401/blacklist" resolver-seviyesi yetki düşmesiyle doğrulanır (token-katmanı 401 ISSUE-05 kapsamı). AC'ler bu derinlikte karşılandı. Review: ayrı agent → APPROVE WITH NITS (nit'ler uygulandı/loglandı).

> 2026-06-06: **roles-permissions ISSUE-08 tamamlandı (oksis-web)** — *Web yetki tüketim primitifleri + rol-izin matrisi* (`.claude/generated-issues/roles-permissions/ISSUE-08`). authStore'a `permVersion` (salt-okunur; login/refresh/`/auth/me/context` ile dolar, 4 auth giriş noktasına bağlandı) eklendi; `perms` zaten `user.permissions`'taydı. `usePermission` **tek hook** olarak korundu + `has`/`hasAny` overload'u eklendi (drift "ikiye bölme" uyarısına uyuldu; tekil `usePermission(perm): boolean` çağrısı bozulmadı). `PermissionGate` inline gate (yetki yoksa **DOM üretmez**, `permission`/`anyOf`/`fallback`) — mevcut route-guard ve eski inline `RequirePermission` ezilmedi. `portals/admin/users/`: `roleApi` (assignable + permissions GET), tenant-scoped `roleKeys`, `useAssignableRoles`, `RolePermissionMatrix` (`roles.manage` gated, shadcn Table, i18n). 18 vitest testi. Stack: shadcn/Tailwind/TanStack v5/Zustand (analiz DevExtreme'i geçersiz). Atama yazma UI'si (AssignRoleModal vb.) identity/ISSUE-23 kapsamında — burada yok. Review: ayrı agent → APPROVE WITH NITS (nit'ler cosmetic/ileri kapsam).

> 2026-06-06: **Rol-izin matrisi görünürlük filtresi + `/admin/roles` ekranı (oksis-api + oksis-web)** — *Kullanıcı talebi: "Okul Yöneticisi Süper Admin'in yetkilerini görmesin."* (1) **API:** `ListRolePermissionsQueryHandler` artık `ITenantContext` + `IAccessTokenContext` alıyor; SuperAdmin hariç aktörün **kendi seviyesi ve altındaki** rolleri döner (`Level <= actorMax`, aktif `RoleAssignment`'lardan; aktif rol yok → boş, fail-closed). Atama filtresiyle (`< actorMax`) tutarlı ama görüntülemede kendi rolü dahil olduğundan `<=`. Privilege-leak kapatıldı (SchoolAdmin → SUPER_ADMIN izinlerini göremez). 3 integration test (SuperAdmin tam matris / SchoolAdmin üst rolleri gizler / aktif-rol-yok boş). (2) **Web:** `/admin/roles` rotası (`roles.manage` gated) + sidebar nav; `RolePermissionMatrix` tablo→**rol kartları + alan (domain) bazında collapsible izin çipleri**'ne yeniden tasarlandı (özet kutucukları, skeleton, i18n domain etiketleri TR + slug). Tasarım sistemi: shadcn/Tailwind/CSS-vars.

> 2026-06-06: **roles-permissions ISSUE-09 tamamlandı (oksis-mobile)** — *Mobil yetki tüketimi + permVersion senkronu* (`.claude/generated-issues/roles-permissions/ISSUE-09`). authStore'a `permVersion` (salt-okunur, secure-store persist, logout reset) + `setPermissions`. `usePermission` web ile **birebir aynı imza** (tekil `boolean` + `has`/`hasAny` overload, tek hook). `PermissionGate` (yetki yoksa render etmez; `permission`/`anyOf`/`fallback`). **Kritik düzeltme (review B1):** account JWT izin taşımaz (TQ-auth-002) → `applyAccountAuthResult` perms'i boş bırakıyordu; `hydrateAccountPermissions` (`/auth/me/context` → perms+permVersion) eklenip **login + switch-profile + switch-season + interceptor refresh** sonrası çağrıldı (web `hydrateAccountPermissions` ile simetrik) — in-session gating artık çalışıyor. `syncPermissions` açılışta sürüm senkronu (sunucu>lokal → sessiz refresh + taze perms). `ContextView.permissions` alanı eklendi. Konum: feature-based (`src/features/auth/...`, drift'e uyuldu). Rol ATAMA ekranı yok (admin-only). 11 yeni jest testi; typecheck + lint temiz. Review: ayrı agent → CHANGES REQUESTED (B1) → düzeltildi. **Not:** `portal-routing.test.tsx` (3 test) temiz ağaçta da kırık (SessionContextBanner/expo-blur native mock) — bu issue ile ilgisiz, pre-existing.

> 2026-05-31: **ISSUE-18 tamamlandı (mobile)** — `LoginScreen` Account akışına geçti (identifier + parola; TCKN client-side bloklanır). `shared/api/client.ts` refresh önce Account `/auth/account/refresh` denenir (legacy fallback); X-Client-Type: mobile header gövde refresh akışını korur. `authStore`'a `AccountContext` eklendi ve `expo-secure-store`'a persist edildi (access in-memory, refresh secure-store, AsyncStorage YASAK). `RootNavigator` `accountContext.activeProfileType`'a göre stack seçer (profile→role mapping: Staff→Admin); çoklu profil + auto-select edilemediyse `ProfileSelectScreen` (≤2 tap). `useAccountLogin` + classify (401/403/409/423/429), `useSwitchProfile`/`useSwitchChild` + `useSelectAllChildren`/`useSwitchSeason`/`useAvailableContexts`/`useAccountChangePassword`/`useAccountLogout` hook'ları. Tenant-scoped React Query keys (`tenantScopedKey`). `account.schema` TR-auth-002 + `applyAccountAuthResult` JWT mapping + "Tümü" branch testi. 13 unit test. Commit: `9a84e68` (oksis-mobile).

---

## ✅ Tamamlanan Yapılar

- **Master data:** `system_roles` (5 — MVP), `permissions` (68), `role_permissions` (164) + deterministik seed. (2026-06-05: rol seti 7→5'e indirildi, bkz. Spec Dışına Çıkılanlar.)
- **Backend (mevcut `User` modeli):** login + refresh akışı, kullanıcı + davet (invitation) handler'ları, `IJwtTokenService`, `IRefreshTokenStore` (InMemory + DB), `IPasswordHasher`, `RefreshTokenCookie`.
- **Web:** `src/modules/identity` + `src/modules/invitations` dikey dilim; admin `users` sayfası.
- **Docs:** 10 dosyanın tamamı teknik analize göre dolduruldu (skeleton/`{{TBD}}` büyük ölçüde kaldırıldı).

## ⏳ Eksik / Bekleyen Yapılar (teknik analiz hedefi)

<!-- Tüm 18 issue tamamlandı: API (1..14), web (15/16/17), mobile (18). -->
- **Permission seed:** `accounts.unlock` + diğer Account permission'ları için yeni migration (operasyonel takvim; modül kodu hazır).
- **SignalR mobile consumer:** Backend forced logout event'leri (ISSUE-12) mobile tarafında ayrı bir entegrasyon görevi olarak Sprint sonrasına alındı; ISSUE-18 kapsamında auth akışı + 401 zincirleme logout devreye girdi, native push-driven logout devam ediyor.
- **Mobile recovery ekranları:** ISSUE-18 OoS gereği forgot/reset/change-password mobile UI'ları sonraki sprint'e alındı; backend endpoint'leri hazır.

## ⚠️ Spec Dışına Çıkılanlar

- **2026-06-06 — roles-permissions ISSUE-06: domain premise eklendi + recurring trigger ertelendi (oksis-api, bu oturum):**
  - **Domain genişletmesi issue'ya dahil edildi:** Issue Scope yalnız job + DI'yı listeliyordu ama premise'i (`RoleAssignment.ValidUntil`/`Expire()`/`Expired`) yoktu (issue'nun kendi "Sync Drift" notu). Önkoşul issue'ya çıkarmak yerine bu issue içinde eklendi (issue "yoksa bu issue ekler" diyordu). **Etki:** `RoleAssignment.Create(...)` imzasına opsiyonel `validUntil` parametresi eklendi (geriye dönük uyumlu; mevcut çağıranlar etkilenmez). `ValidUntil`'i set eden komut/endpoint **bu issue kapsamında değil** (OoS: UI gösterimi) — alan hazır, yazma yolu sonraki issue'da.
  - **Recurring Hangfire tetikleme (cron) eklendi (2026-06-06, kullanıcı talebi):** Api host'ta `HangfireSetup` (`Oksis.Api/Extensions/HangfireSetup.cs`) — **config-gated**: yalnız `Hangfire:Enabled = true` + geçerli SQL connection string varsa Hangfire server + SQL storage kurulur ve `expire-role-assignments` recurring job'u `Hangfire:Cron:ExpireRoleAssignments` (default `0 3 * * *`, Europe/Istanbul TZ) ile register edilir; aksi halde mevcut `InProcessBackgroundJobClient` fallback'i korunur (dev/test/CI hangfire şeması olmadan çalışır). `Hangfire.AspNetCore`/`Hangfire.SqlServer` (1.8.23, zaten Infrastructure'da referanslı) Api csproj'a eklendi. **Kapsam (kullanıcı talebiyle genişletildi 2026-06-06):** başlangıçta yalnız ExpireRoleAssignmentsJob register edildi; ardından **tüm sibling sweep'ler de register edildi** — `expire-stale-invitations` (günlük 02:00), `refresh-token-cleanup` (günlük 03:15), `otp-challenge-cleanup` (günlük 03:30), `audit-retention-enforcer` (aylık 1. gün 02:00). Cron'lar `Hangfire:Cron:<Key>` ile override edilir, hepsi Europe/Istanbul, aynı-dakika çakışması olmayacak şekilde kademeli (background-job-rules §9). **Not:** prod appsettings'te `Hangfire:Enabled` true yapılmalı; `Hangfire:PrepareSchema=false` ile şema CI/CD script'ine bırakılabilir. Dashboard bilinçli olarak açılmadı (güvenlik yüzeyi; istenmedi).
  - **Cache invalidation `IPermissionResolver.InvalidateAsync` değil, gerçek portlar:** Issue taslağı `IPermissionResolver.InvalidateAsync` diyor; gerçek isimler `IPermissionCacheBuilder.InvalidateAllForPersonAsync` + `IPermissionVersionBumper.BumpForPersonsAsync` (ISSUE-03/05 drift'i, `RevokeRoleAssignment` handler'ıyla aynı yetki-düşürme deseni).
  - **`revokeSessions: false` (admin iptalinden bilinçli farklı):** `RevokeRoleAssignment` (elle/acil iptal) `revokeSessions: true` ile refresh token'ları da revoke eder. Otomatik süre dolumu acil değildir; yalnız `perms_ver` bump edilir → eski access token sonraki istekte 401 → sessiz refresh → süresi dolan rol düşmüş olarak yeniden çözülür. Kişinin başka aktif rolleri sürebileceğinden tam logout yerine şeffaf yenileme seçildi (AC "sonraki istekte yetki düşer" karşılanır). **Karar:** review (başka agent) önerisi + bu oturum.
- **2026-06-06 — roles-permissions ISSUE-05 kapsam uyarlamaları (oksis-api, kullanıcı onayı bu oturum):**
  - **`perms` claim JWT'ye EKLENMEDİ (TQ-auth-002'ye uyum):** Issue Scope "JWT üretimine `permVersion` + `perms` claim eklenmesi" diyor; ancak 2026-05-31 TQ-auth-002 kararı izinleri JWT'den **çıkardı** (token sade; perms `/auth/me/context` + Redis runtime'dan). Sadece `perms_ver` claim taşınır (zaten vardı); `perms` listesi bilinçli olarak JWT'ye geri konmadı. **Etki:** AC#1'in "perms claim" kısmı `/auth/me/context` ile karşılanır.
  - **Acil iptal mekanizması perms_ver bump'a dayanır, admin-revoke'ta jti blacklist'e değil:** Bir admin BAŞKA kullanıcının rolünü iptal ettiğinde o kullanıcının aktif access token jti'si sunucuda bilinmediğinden blacklist'e yazılamaz; anlık kesme `perms_ver` bump + DB/Redis aynası ile sağlanır (eski token bir sonraki istekte 401) + refresh revoke. jti blacklist yalnız self-logout/parola-değişimi (jti elde) yollarında kullanılır (mevcut davranış). **Etki:** AC#4 "15 dk blacklist" ifadesi admin-revoke'ta perms_ver-401 ile eşdeğer anlık kesme olarak karşılanır.
  - **`access-blacklist:{school}:{jti}` SchoolId-prefix'i eklenmedi:** AC#8 permVersion aynası SchoolId taşır (`permver:{school}:{account}` ✓). Mevcut blacklist anahtarı `blacklist:jti:{jti}` (ISSUE-05/2026-05-31) globally-unique GUID jti kullandığından cross-tenant çakışma imkânsız; bu issue'da yeniden mimarileştirilmedi. **Gerekçe:** fonksiyonel tenant-güvenliği zaten var, yüzeyi büyütmemek için dokunulmadı.
  - **`RolePermissionsChangedEvent` producer'ı dormant:** Consumer (`INotificationHandler`) + testleri yazıldı (AC#6). Master matris seed-only (ISSUE-01) olduğundan runtime raise noktası yok; runtime matris düzenleme geldiğinde tek satırla raise edilecek. **Karar:** kullanıcı "implement it now" dedi → consumer hazır, trigger ertelendi.
  - **HTTP-seviyesi 401 e2e testi ertelendi:** Gate stale-mantığı unit-covered (`PermissionVersionGateTests`); `OnTokenValidated` ince adaptördür. WebApplicationFactory + SQL fixture gerektiren uçtan-uca 401/`/auth/me/context` testi, auth wiring doğrulamasının yapıldığı integration suite'ine (ISSUE-14 sınırı) bırakıldı.

- **2026-06-05 — Master rol seti MVP'de 5 role indirildi (oksis-api, Issue #1):** Seed 7 sistem rolü içeriyordu (`SUPER_ADMIN, SCHOOL_ADMIN, VICE_PRINCIPAL, TEACHER, COUNSELOR, PARENT, STUDENT`); `permission-matrix.md` ise 8 rol listeliyordu (ek olarak `SchoolStaff, Secretary, Accountant`). Kullanıcı kararıyla MVP rol seti **5 role sabitlendi**: `SuperAdmin, SchoolAdmin, Teacher, Parent, Student`. `VicePrincipal` + `Counselor` seed'den kaldırıldı; `SchoolStaff/Secretary/Accountant` hiç eklenmemişti — hepsi **MVP sonrasına** ertelendi (gerekirse ilişki-bağlı yeniden eklenir). Etki: `SystemRoleSeedData` 7→5, `RolePermissionSeedData` 200→164 eşleşme (VicePrincipal 29 + Counselor 7 kaldırıldı), izin katalogu (68) değişmedi, `MasterSeedIds` ertelenen Guid'leri silindi, `IdentityDevSeeder` Müdür Yardımcısı dev kullanıcısı VicePrincipal→SchoolAdmin. Migration `20260605_reduce_system_roles_to_mvp_five` (tersine çevrilebilir DeleteData). `MasterRoleSeedTests` (5 test) seed bütünlüğünü kilitler. **Gerekçe:** MVP kapsam sadeleştirmesi; çok rollü matris hedef olarak korunur. **Karar:** kullanıcı onayı (bu oturum). `permission-matrix.md` MVP notuyla güncellendi. **Not:** Mevcut dev DB'ler migration gerektirir (`ef database update`); kaldırılan rollere ait RoleAssignment varsa FK temizliği gerekebilir.
- **2026-06-01 — `PersonAccessGuard` ABAC kapsam kontrolü izin-tabanlı yapıldı + `users.view-all` izni eklendi (oksis-api):** Account-tabanlı token'la (`active_profile_type=Staff`, SCHOOL_ADMIN) `GET /users/persons/{id}` **403** dönüyordu. Kök neden TQ-auth-002 ile aynı sınıf: `PersonAccessGuard` geniş kapsamı `currentUser.Roles` (= JWT `ClaimTypes.Role`) üzerinden çözüyordu; account token'da rol claim'i yok → `roles` her zaman boş → `_broadRoles` bypass'ı asla tetiklenmedi → SchoolAdmin dahil herkes default-deny'a düştü (yalnız self geçiyordu). Ayrıca `_broadRoles` string'leri gerçek rol kodlarıyla uyuşmuyordu (`"SchoolStaff"`/`"Secretary"` hiç yok; `"SchoolAdmin"`≠`SCHOOL_ADMIN`) ve `Accountant` ödeme-kapsamı dalı hem hiç var olmayan bir role bağlıydı hem de `currentPersonId`'yi referans almadığı için IDOR açığı taşıyordu. **Çözüm (Option A):** guard rol string'lerini tamamen bıraktı; geniş kapsam = `IPermissionReader.HasPermissionAsync("users.view-all")` (RBAC ile aynı runtime kaynağı), kalan dallar gerçek ilişki kayıtlarına dayanır (self / veli→CanViewInfo'lu çocuk / öğretmen→kendi sınıfı). IDOR'lu `Accountant` ödeme dalı kaldırıldı (finans rolü/izni doğduğunda ilişki-bağlı yeniden eklenir). Yeni `users.view-all` izni eklendi (SUPER_ADMIN + SCHOOL_ADMIN; migration `20260601_add_users_view_all_permission`). **Gerekçe:** sistemin permission-matrix felsefesiyle hizalı, Application katmanına rol string'i sızmaz. **Karar:** kullanıcı onayı (bu oturum). permission-matrix.md güncellendi. **Not:** `IPersonAccessGuard`'ın tek tüketicisi `GetPersonDetailQueryHandler`; mevcut dev DB'ler migration + (RoleAssignment için) seed gerektirir.
- **2026-05-31 — Account-login izin çözümleme zinciri tamamlandı + TQ-auth-002 karara bağlandı (oksis-api + web):** Identity birleştirmesi sonrası web+mobil admin okul ayarlarında (ve tüm izin-korumalı uçlarda) **403** alınıyordu. İki kök neden: (1) `AccountTokenIssuer` izinleri JWT'ye basmıyordu (tasarım gereği Redis'e taşındı) ama yetkiyi okuyan iki yer — MediatR `AuthorizationBehavior` ve ASP.NET `PermissionRequirementHandler` — hâlâ JWT `permissions` claim'ini okuyordu → her zaman boş. Çözüm: `IPermissionReader`/`PermissionReader` (Redis cache → miss'te `AccountPermissionResolver` ile DB'den kur + geri yaz; legacy claim fallback) eklendi, iki yetki yolu da buna geçti (`PermissionRequirementHandler` Singleton→Scoped). (2) `IdentityDevSeeder` hiç `RoleAssignment` üretmiyordu → resolver 0 izin döner. Çözüm: `Provision`'a roleIds eklendi (müdür→SchoolAdmin, yrd→VicePrincipal, öğretmen→Teacher[+dual Parent], öğrenci→Student, veli→Parent), sentetik `DevSeasonId` (RoleAssignment.SeasonId'in AcademicSession FK'si yok). **Web:** izinler artık JWT'de olmadığından `ContextView`'e `permissions[]` eklendi; web login/refresh/profil-sezon switch sonrası `/auth/me/context`'ten izinleri çekip store'a yazıyor (`hydrateAccountPermissions`). **Karar (TQ-auth-002):** izinler Redis+DB runtime resolution, JWT'de değil; istemciye `/auth/me/context` ile ulaşır (open-questions arşivine taşındı). **Not:** Mevcut dev DB'ler seed guard nedeniyle atamasız kalır → DB sıfırlanmalı (`ef database drop` + `database update` + run).
- **2026-05-31 — Çok profilli 409 açığı login `profileType` hint'iyle kapatıldı (oksis-api):** `409 NEEDS_PROFILE_SELECTION` token döndürmüyordu ama `/auth/switch-profile` Bearer token istiyordu → yeni dual kullanıcı oturum açamıyordu. Çözüm: `AccountLoginCommand`/`AccountLoginBody`'ye opsiyonel `profileType` eklendi; istemci 409 sonrası aynı login'i seçtiği profille tekrarlar (`ContextResolver` önceliği: hint > LastActiveProfileType > 409; açık ama sahip-olunmayan değer yine 409). switch-profile oturum-içi manuel geçiş için korunur. **Gerekçe:** ui-flows "409 → switch-profile" diyordu ama bu interim "seçim token'ı" + authorization policy gerektiriyordu; re-login hint'i daha küçük yüzey, ek token/policy yok (kullanıcı kararı). Doküman güncellendi (api-contracts/ui-flows/business-rules). **Web hizalaması (oksis-web):** `LoginPage` 409'da artık `/profile-select`'e yönlendirmiyor; profil seçimini **inline** gösterip aynı credential'larla `profileType` ekleyerek tekrar login ediyor (parola yalnız bileşen belleğinde, router state'iyle taşınmaz). `ProfileSelectPage`/`/profile-select` artık 409 akışında kullanılmıyor (orphan, zararsız); `switch-profile` yalnız oturum-içi `ProfileSwitcher` için kalır.
- **2026-05-31 — account-login success-path `DbUpdateConcurrencyException` düzeltildi (oksis-api):** Tek profilli account-login `/auth/account/login` başarılı parolada 500 dönüyordu. Kök neden: `RefreshTokenConfiguration`'da `Id` için `ValueGeneratedNever()` eksikti → Guid PK convention'la `ValueGeneratedOnAdd` kalıyor, mevcut (tracked) `Account`'un `RefreshTokens` koleksiyonuna eklenen yeni token non-default key yüzünden `Added` yerine `Modified` sanılıyor, UPDATE üretiliyor, satır olmadığı için concurrency hatası. Düzeltme: `Id.ValueGeneratedNever()` (Person/Profile/ConsentRecord ile tutarlı). Aynı hata `/auth/refresh` (RotateRefreshToken) için de geçerliydi, kapsandı. Boş migration `20260531_refresh_token_id_value_generated_never` (yalnız model snapshot).
- **2026-05-31 — `users` şeması `identity`'ye birleştirildi + dev seed yeni modele geçti:** Kullanıcı yönetimi tabloları `[users]` → `[identity]` taşındı; dev seed artık yeni `Account/Person/Profile` modeliyle 3 okul için gerçekçi loginable kadro üretir. Detay: `users/completion_status.md → Spec Dışına Çıkılanlar`. Bu, OQ-identity-001 (Account vs User) için fiilen **Account yolunun** dev'de canlı olduğunu doğrular (legacy `User` seed'i kaldırıldı).
- **2026-05-30 — Modül yerleşimi:** Teknik analiz ayrı projeler (`Oksis.Identity.Domain` vb.) önerir; bu repo modüler monolit olduğundan docs **mevcut `Oksis.Domain/Modules/Identity` alt klasör** yapısına göre yazıldı. Sebep: CLAUDE.md klasör kuralı. Onay: docs güncellemesi sırasında varsayıldı; mimar teyidi bekliyor (OQ-identity-002).
- **2026-05-30 — Account vs User:** Teknik analizin `Account` aggregate'i hedef olarak dokümante edildi, ancak mevcut kod `User` üzerinde çalışıyor. Hangi yolun seçileceği OQ-identity-001'de açık; karar verilene kadar bu bir **dokümante edilmiş hedef**, uygulanmış gerçek değil.
- **2026-05-31 — OQ-identity-001 karar:** Option A (yeni `Account` aggregate) seçildi ve domain katmanına eklendi; mevcut `User` aggregate'i auth sorumluluğunu **geçici olarak** koruyor. Sebep: teknik analize tam uyum (clean Person/Account ayrımı); users modülünde Person yazma tamamlandıkça `User`'dan PasswordHash/lockout/FailedLogin alanları emekli edilecek. Onay: mimar (auto-mode session içinde varsayıldı, mimar onayı sonradan teyit edilmeli).
